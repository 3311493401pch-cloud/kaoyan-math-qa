services:
  - type: web
    name: kaoyan-math-qa
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false